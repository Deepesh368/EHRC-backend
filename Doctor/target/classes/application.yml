server:
  port: 8002
spring:
  application:
    name: doctor-service

  datasource:
    url: jdbc:mysql://localhost:3306/hospital_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    enabled: false